required: time, perl, perl XML::Simple, paraccel utils

1.
copy "paraccel" directory to /usr/local/zabbix/etc/ on your main ParAccel server

2.
file zabbix_paraccel.sh:

check PATH to psql (PATH=/home/paraccel/padb/rel/bin/:$PATH in file)
check connect string (pass,user,db and etc) (Q1="psql -h 127.0.0.1 -p 5439 padb_harvest --username=paraccel -A -t -c")

file zabbix_paraccel_nodes.sh:
check PATH to psql (PATH=/home/paraccel/padb/rel/bin/:$PATH in file)
check connect string (pass,user,db and etc) (Q1="psql -h 127.0.0.1 -p 5439 padb_harvest --username=paraccel -A -c")

file zabbix_paraccel_nodes_cqi.pl:
check PATH to cqi (my $CMD="/home/paraccel/padb/rel/bin/cqi csm -v xml"; in file)

3.
copy or link file userparams_paraccel.conf to your "zabbix_agentd.conf.d" directory (/etc/zabbix/zabbix_agentd.conf.d by default)

4.
copy or link file "zabbixparaccelcrontab" to /etc/cron.d/

5. 
reload cron (/etc/init.d/crond reload)
reload/restart zabbix_agentd (/etc/init.d/zabbix_agentd reload or /etc/init.d/zabbix-agentd reload or etc)

6.
on zabbix UI 
export paraccel_2014.01.28.xml template
